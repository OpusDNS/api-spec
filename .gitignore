/node_modules
scalar/node_modules/
.DS_Store
openapi.yaml

# Scalar CLI local cache (defensive; current versions cache in ~/.scalar)
.scalar/
scalar/.scalar/
.idea
# Python build artefacts (TLD knowledge base generator)
__pycache__/
*.py[cod]

# Sibling repos checked out into this tree by the sync workflows.
# mcp/ is created by "Publish MCP docs to api-spec" in OpusDNS/opusdns-mcp, which
# checks this repository out at the workspace root and its own alongside it.
/mcp/
/tld-specifications/
